#include <iostream>

// The functions std::min, std::max and std::minmax, defined in the header <algorithm>, act on values and initialiser lists and give you the requested value back as result.
#include <algorithm>


/*
here we will make use of the following:
1. auto : you initialize a var and at runtime compiler will decide its type, but initializing the var is a must
--
auto f = [&](int x, int y) { return fudgeFactor * (x + y); };
// the expression to the right of the "=" is the lambda expression (i.e., "the lambda"),

// The runtime object created by that expression is the closure.
*/

int main(int argc, char const *argv[]) {

  return 0;
}
